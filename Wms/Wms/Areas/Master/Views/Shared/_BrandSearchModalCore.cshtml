@* ブランド検索の検索結果(Ajax.BeginFormで検索結果だけ差し替えるため部分ビューにする) *@
@model IList<Wms.Areas.Master.ViewModels.BrandSearchModal.BrandViewModel>

<h1>@Wms.Areas.Master.Resources.SharedResource.BrandTitle</h1>
<div class="sublbarea">
</div>
<div class="Container">
    <div>
        <div class="tb_scroll">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th class="bg-grey w_checkbox"></th>
                        <th class="bg-grey">
                            No
                        </th>
                        <th class="bg-grey">
                            @Html.DisplayNameFor(model => model.FirstOrDefault().Brand.BrandId)
                        </th>
                        <th class="bg-grey">
                            @Html.DisplayNameFor(model => model.FirstOrDefault().Brand.BrandName)
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < Model.Count(); i++)
                        {
                    <tr>
                        <td class="chk_cell">
                            <label>
                                @Html.RadioButton("SelectIndex", i.ToString(), new { @id = "BrandSelectIndex" + i.ToString(), @class = "radio_input" })
                                <span class="radio_parts"></span>
                            </label>
                        </td>
                        <td class="text-right w_no">
                            @((i + 1).ToString())
                        </td>
                        <td>
                            @Html.DisplayFor(model => model[i].Brand.BrandId)
                        </td>
                        <td>
                            @Html.DisplayFor(model => model[i].Brand.BrandName)
                        </td>
                        @Html.HiddenFor(model => model[i].Brand.BrandId)
                        @Html.HiddenFor(model => model[i].Brand.BrandName)
                    </tr>
                        }
                    <tr>
                </tbody>
            </table>
        </div>
    </div>
</div>